{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bunx prettier --write \"$CLAUDE_FILE_PATH\" 2>&1 || echo \"WARNING: Prettier failed on $CLAUDE_FILE_PATH\" >&2; exit 0",
            "timeout": 5000
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "echo \"$CLAUDE_FILE_PATH\" | grep -qE '(^|/)\\.env(\\.|$)' && ! echo \"$CLAUDE_FILE_PATH\" | grep -qE '\\.example$' && echo \"BLOCK: .env files are protected: $CLAUDE_FILE_PATH\" >&2 && exit 1 || exit 0"
          }
        ]
      }
    ]
  }
}
